new_list = [1, 2, 3, 4, 5]
result = new_list[6]

# Access is a contant time operation on an array
# or python list bcos the array stores references to
# each of those object


# 2. Searching an item in an array
if 1 in new_list:
    print(True)

for n in new_list:
    if n == 1:
        print(True)

        break
